# Capability schema fixture

`privacy-capabilities.schema.json` vendors the schema content from
`zcash-privacy-stack/interfaces/privacy-capabilities.schema.json` in the
adjacent `zcash-privacy-stack` repository as of 2026-08-28. It is test data only;
production code has
no cross-repository runtime dependency.
